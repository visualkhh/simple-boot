rootProject.name = 'simple-boot'
String[] simples = ['simple', 'simple-h2', 'simple-web', 'simple-hibernate']
String[] fronts = ['simple-front']
String[] projects = ['example']

simples.each {
    include it
    def projectDir = new File(rootDir, it)
    if (!projectDir.exists()) {
        projectDir.mkdirs()
    }
}
fronts.each {
    include it
    def projectDir = new File(rootDir, it)
    if (!projectDir.exists()) {
        projectDir.mkdirs()
    }
}
projects.each {
    include it
    def projectDir = new File(rootDir, it)
    if (!projectDir.exists()) {
        projectDir.mkdirs()
    }
}
